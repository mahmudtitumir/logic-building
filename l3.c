#include <stdio.h>

int main() {
    /*
    // code 04
    int r;
    scanf("%d", &r);
    printf(" The Radius is %f", 3.14 * r * r);
    // code 05
    int a, b, c;
    float avg;
    scanf("%d %d %d", &a, &b, &c);
    avg = (a + b + c) / 3.0;
    printf("Average is %f", avg);
    */
    // code 06
    int a;
    scanf("%d", &a);
    printf("Last digit is %d", a % 10);
    return 0;
}